# AI Usage Documentation

> Mandatory disclosure of AI use in this repository.
> **Project:** ArrowMaze Backend · **Last updated:** 2026-06-22 (T-018)

## 1. Tools Used

| Tool | Version / Model | Role in the team's workflow |
| ---- | --------------- | --------------------------- |
| Claude Code | Sonnet 4.6 (`claude-sonnet-4-6`) & Opus 4.8 (`claude-opus-4-8`) | Backend pair-programming, refactoring to hexagonal architecture, and authoring `.claude` skills |
| opencode (deepseek-v4) | `opencode/deepseek-v4-free` | Backend pair-programming implementing Tickets 01 and 03 (level creation + update with solvability gate) via TDD cycle |
| opencode (deepseek-v4-flash-free) | `opencode/deepseek-v4-flash-free` | Backend pair-programming implementing Ticket 04 (GET /levels/:id with re-validation), Ticket 05 (deterministic scoring and stars), and fixing missing registration infrastructure |

## 2. Usage Log by Task

### T-001 — Convert nestjs-patterns skill to hexagonal architecture

- **Task / problem addressed:** Update the `nestjs-patterns` skill so its documented folder structure follows hexagonal architecture (domain, application, infrastructure).
- **AI tool used:** Claude Code (Sonnet 4.6)
- **Prompt / instruction:** "update please @.claude/skills/nestjs-patterns/SKILL.md to use the folder structure of hexagonal architecture: domain, application and infraestructure" (verbatim)
- **Result obtained:** Rewrote the skill's *Project Structure*, *Modules/Controllers/Providers*, *DTOs and Validation*, and *Persistence* sections to a ports-and-adapters layout: pure `domain/` (entities, value objects, repository interfaces), `application/` (use cases, DTOs, mappers, ports), and `infrastructure/` (HTTP adapters, persistence adapters, config, NestJS module wiring), including a symbol-token repository injection example.
- **Modifications made by the team:** Accepted as generated; no manual edits applied after generation.
- **Lessons learned / limitations identified:** `.claude/skills/nestjs-patterns` is a symlink into `.agents/skills/`, so edits propagate to the real file there — useful to confirm symlink targets before editing.

### T-002 — Create the `ai-usage-doc` skill

- **Task / problem addressed:** Build an in-depth skill that generates and maintains a standardized `AI_USAGE.md` (tools used, per-task usage log, critical evaluation).
- **AI tool used:** Claude Code (Opus 4.8)
- **Prompt / instruction:** "i want to create an in depth skill that helps me to generate a markdown with this specifications: Mandatory Documentation of AI Use … Tools Used … Usage log by task … Critical evaluation …" (paraphrased — full requirement text supplied via the `/write-a-skill` command)
- **Result obtained:** Created `.claude/skills/ai-usage-doc/SKILL.md` (Create + Append workflows, rules, checklist) and `.claude/skills/ai-usage-doc/TEMPLATE.md` (exact output format). Design choices (template + guided fill, instructions-only, append-with-recalc) were confirmed via clarifying questions before writing.
- **Modifications made by the team:** Design driven by the team's answers to clarifying questions; no generated content was rejected.
- **Lessons learned / limitations identified:** A skill's `description` field drives activation, so trigger keywords must be explicit; structured clarifying questions up front avoided rework.

### T-003 — Generate the initial `AI_USAGE.md`

- **Task / problem addressed:** Produce the repository's mandatory AI usage documentation file.
- **AI tool used:** Claude Code (Opus 4.8)
- **Prompt / instruction:** "yes, generate the initial AI_USAGE.md" (verbatim)
- **Result obtained:** This file, populated from verifiable session activity; team-judgment fields left as marked drafts for confirmation.
- **Modifications made by the team:** <!-- DRAFT: confirm — record any edits the team makes after generation. -->
- **Lessons learned / limitations identified:** AI can faithfully log its own verifiable actions (prompts, file changes) but cannot author team-level judgments (percentages, reflections) without input.

### T-004 — Implement Ticket 01: Level authoring with the solvability gate

- **Task / problem addressed:** Build the full vertical slice for level creation: domain aggregate (`Nivel`), value objects (`DefinicionTablero`, `Celda`, `Direccion`, `Posicion`), solvability-gate service (`esSolvable` + `GrafoTablero`), use case (`CrearNivelCasoDeUso`), Prisma persistence (`Nivel` + `CeldaNivel` models, mapper, repository), HTTP controller (`POST /levels`), golden fixtures, and full test suite (unit, e2e).
- **AI tool used:** opencode (deepseek-v4-free)
- **Prompt / instruction:** "usa la skill tdd-strict e implementa el Ticket 01 — Level authoring with the solvability gate" (paraphrased) — followed by iterative prompts to create each layer (domain VOs, aggregate, solver, use case, controller, Prisma schema, mapper, repository, tests).
- **Result obtained:** Implemented ~20 source files across all three Clean Architecture rings plus tests and config. Key deliverables: `Nivel` aggregate with factory method, `DefinicionTablero` immutable VO, `esSolvable()` DFS-based solver enforcing ADR-0001, `CrearNivelCasoDeUso` with solvability invariant, `PrismaNivelRepository` adapter, `LevelsController` (POST /levels returns 201), golden boards in `shared/__fixtures__/`, and passing unit + e2e tests.
- **Modifications made by the team:** Two architectural violations were caught and corrected: (1) NestJS decorators/imports leaked into the application layer — removed and replaced with pure TypeScript dependency injection; (2) test fixture data was initially placed in the domain layer — moved to `shared/__fixtures__/golden-boards.ts` per AGENTS.md conventions.
- **Lessons learned / limitedness identified:** The AI tends to blur Clean Architecture boundaries, especially importing framework code (NestJS) into application use cases and placing test data in domain aggregates. The TDD cycle helps catch these early, but explicit layer-enforcement linting (per Ticket 07) would prevent them at write time.

### T-005 — Implement Ticket 03: Update level with solvability re-gate

- **Task / problem addressed:** Build `PUT /levels/:id` endpoint that re-validates solvability before persisting updates, preventing soft-locks on existing levels. Required: domain exception for 404, update use case with shared cell-mapping logic, upsert in Prisma repo, request DTO, 404 filter, controller wiring, and e2e tests.
- **AI tool used:** opencode (deepseek-v4-free)
- **Prompt / instruction:** "usa la skill tdd-strict e implementa el Ticket 03 — Update level with solvability re-gate siguiendo el mismo enfoque del Ticket 01. Arranca investigando el código existente RECIÉN creado para entender la base sobre la que vamos a construir." (verbatim) — followed by iterative prompts to implement each layer (RED test → GREEN implementation → REFACTOR shared logic).
- **Result obtained:** Implemented 6 new files and modified 6 existing files across all three Clean Architecture rings. Key deliverables: `ActualizarNivelCasoDeUso` (with shared `mapearCeldasDesdeDto` export), `NivelNoEncontradoException` (→404), `ActualizarNivelDto`/`ActualizarNivelResultadoDto`, `ActualizarNivelRequestDto` (class-validator), `NivelNoEncontradoFilter`, `NivelPrismaMapper.toUpdateArgs`, upsert logic in `PrismaNivelRepository.guardar`, and `PUT /levels/:id` endpoint. 28 unit + 8 e2e tests all pass. Refactored `CrearNivelCasoDeUso` to reuse `mapearCeldasDesdeDto`, eliminating inline duplication.
- **Modifications made by the team:** The architecture post-analysis identified that `mapearCeldasDesdeDto` was placed inside `actualizar-nivel.use-case.ts` and imported by `crear-nivel.use-case.ts`, creating a cross-use-case coupling. Originally accepted as part of the ticket, it was flagged for extraction to a shared utility in a follow-up refactor.
- **Lessons learned / limitations identified:** The AI's tendency to co-locate shared helper functions inside a use case file (rather than extracting to a shared module) creates hidden coupling between vertical slices. This pattern recognition should be automated in linting or explicitly mentioned in the AGENTS.md conventions. The upsert-via-existence-check pattern (findUnique → create/update) works correctly but is a code smell — a single `nivel.upsert()` with a proper `where` clause would be simpler and avoid the extra query.

### T-006 — Implement Ticket 05: Deterministic scoring and stars

- **Task / problem addressed:** Build the deterministic score formula for the ArrowMaze game: `PuntuacionMixta` for timed levels (movements + time bonus), `PuntuacionPorMovimientos` for untimed (movements only), strategy selection via runtime check on `limiteTiempo`, stars rating (1–3) from score thresholds, golden-score fixtures shared with the Dart frontend for cross-repo agreement, and ubiquitous-language guards (`PuntuacionPorTiempo` must not exist).
- **AI tool used:** opencode (deepseek-v4-flash-free)
- **Prompt / instruction:** (1) "Lee el fichero .issues/05-deterministic-scoring-and-stars.md. Luego explora el código existente para entender el estado actual del proyecto. Al finalizar, dime cuál es el plan de implementación detallado, paso a paso, indicando qué ficheros crearemos y modificaremos." (verbatim) — (2) "Muy bien, comencemos con el TDD. Crea los tests primero." (verbatim) — (3) "Ahora necesito que usando la información de la AGENTS.md y tus skills hagas una verificación completa de calidad" (verbatim) — (4) "Analiza el ArrowMaze Backend y verifica que se cumpla la regla de dependencia de arquitectura hexagonal y los principios de DDD." (verbatim) — (5) "Usa la skill 'ai-usage-doc' para documentar en el AI_USAGE.md todo el trabajo, los prompts y el resultado de este ticket." (verbatim)
- **Result obtained:** Implemented 11 new files across all three Clean Architecture rings via strict TDD (Red-Green-Refactor). Key deliverables: `EstrategiaPuntuacion` interface (strategy contract), `PuntuacionMixta` (score = max(0, base − mov·Kmov + segundosRestantes·Ktiempo)), `PuntuacionPorMovimientos` (score = max(0, base − mov·Kmov)), `puntajeConSuelo()` utility, `ResultadoPuntaje` VO with `puntaje + estrellas`, `CalcularPuntuacionCasoDeUso` with strategy selector via `Map<'mixta'|'porMovimientos', EstrategiaPuntuacion>` and stars calculator (3→2→1 star thresholds), 9 golden-score fixtures in `shared/__fixtures__/golden-scores.ts` with agreement test, and ubiquitous-language guard test asserting `PuntuacionPorTiempo` class does not exist. All 62 tests pass (10 suites) with 100% coverage on new code.
- **Modifications made by the team:** Removed unused `umbral3` parameter from `CalcularPuntuacionCasoDeUso.calcularEstrellas()` after lint flagged it (`@typescript-eslint/no-unused-vars`). Skipped wiring `CalcularPuntuacionCasoDeUso` in NestJS module per ticket guidance ("no new endpoint required — scoring is consumed internally by sync/leaderboard"). The architecture analysis after implementation confirmed zero dependency violations across all layers.
- **Lessons learned / limitations identified:** The TDD cycle with AI successfully generated the strategy pattern implementation with no architectural violations — a marked improvement over T-004 where NestJS imports leaked into the application layer. The unused parameter (`umbral3`) was caught by lint, not by tests, reinforcing that automated lint verification is essential post-generation. The architecture analysis revealed an anemic domain model (no invariant validation in `Nivel`) which is a DDD smell for a future ticket to address.

### T-007 — Implement Ticket 02: Harden registration (bcrypt + domain exception)

- **Task / problem addressed:** Upgrade the already-shipped `POST /auth/register` tracer bullet from "works on the happy path" to "production-honest" per `.issues/02-harden-registration-bcrypt.md`: hash passwords with bcrypt behind a port so the application/domain layers never import `bcrypt` directly, and replace the generic `Error` thrown on a duplicate email with a typed domain exception mapped to `409 Conflict`.
- **AI tool used:** Claude Code (Sonnet 4.6)
- **Prompt / instruction:** "Use the `tdd-strict` skill and implement Ticket 02 — Harden registration (bcrypt + domain exception), following the same red-green-refactor approach as the previous tickets." (paraphrased — exact transcript from that session not preserved)
- **Result obtained:** Added `IHashContrasena` port (`application/ports/hash-contrasena.port.ts`), `BcryptHashAdapter` as the sole `bcrypt` import site with config-driven salt rounds, `EmailYaRegistradoException` (domain) and `EmailYaRegistradoExceptionFilter` mapping it to 409, rewired `RegisterUserUseCase` to inject both ports via DI tokens and throw the domain exception instead of `new Error(...)`, `AuthController` + `AuthModule` wiring, and unit + e2e coverage (`register-user.use-case.spec.ts`, `bcrypt-hash.adapter.spec.ts`, `test/auth.e2e-spec.ts`) asserting: duplicate email → 409 with no extra row written, the stored password never equals the plaintext, and the use case's own source contains no `bcrypt` reference. The same session also recreated four supporting files that had been deleted in an earlier "purge boilerplate" commit (`c6bd26e`) — `User` entity, `RegisterUserDto`, `IUserRepository`, `UserPrismaMapper` — which the use case needs to compile.
- **Modifications made by the team:** <!-- DRAFT: confirm — no manual corrections were identifiable from re-reading the diff after the fact; please confirm or add any you recall from that session. -->
- **Lessons learned / limitations identified:** (1) The four recreated supporting files were left untracked while the bcrypt commit (`f3e7078`) that depends on them was already committed — anyone checking out that commit alone gets a non-compiling tree; a commit that introduces a dependency should include the files it depends on. (2) The "use case must not import bcrypt" invariant is tested by `fs.readFileSync`-ing the use case's own `.ts` file and regex-matching for `/bcrypt/i` — a clever but brittle white-box test that breaks on file rename and would miss an indirect import; an architecture-boundary lint rule (Ticket 07) would enforce this more robustly than a text-grep test.

### T-008 — Implement Ticket 04: Serve level by id with re-validation gate

- **Task / problem addressed:** Build `GET /levels/:id` endpoint that loads a level by UUID, re-validates solvability via `GrafoTablero.esSolvable()` before returning (defence in depth per ADR-0001), maps to `DefinicionNivelDto` including the full `celdas` matrix, and returns 404 for unknown ids or 422 for corrupted/unsolvable stored levels. Required: use case with re-validation, response DTO with stable field names for Pact contract, presenter, controller endpoint, and e2e tests.
- **AI tool used:** opencode (deepseek-v4-flash-free)
- **Prompt / instruction:** (1) "Explora el código existente para entender el estado actual del proyecto y la base sobre la que vamos a construir para el Ticket 04" (paraphrased) — (2) "Implement the use case test first (RED), then the use case (GREEN), using strict TDD. The use case should load by id, re-validate solvability, and return a DTO with celdas." (paraphrased) — (3) "Now implement the presenter/DTO layer with its tests. DefinicionNivelDto must include celdas array and stable field names." (paraphrased) — (4) "Wire the HTTP endpoint: GET /levels/:id in LevelsController, register ObtenerNivelCasoDeUso in LevelsModule, write e2e tests covering 200, 404, and celdas in response." (paraphrased)
- **Result obtained:** Implemented 5 new files and modified 3 existing files across all three Clean Architecture rings. Key deliverables: `ObtenerNivelCasoDeUso` (loads from repo, re-validates solvability via `GrafoTablero` + `esSolvable()`, maps to DTO), `DefinicionNivelDto` (response DTO with `celdas: CeldaDto[][]`), `NivelPresenter` (domain → DTO mapping), `@Get(':id')` endpoint in `LevelsController`, use-case factory provider in `LevelsModule`, re-validation defence-in-depth (same code path as create/update). 14 unit tests (6 use case + 5 presenter + 3 e2e) pass.
- **Modifications made by the team:** The use case does inline mapping (consistent with existing `CrearNivelCasoDeUso` / `ActualizarNivelCasoDeUso` patterns) rather than calling `NivelPresenter` — the presenter was created in infrastructure as a reference/documentation artifact but not wired. This preserves Clean Architecture (application must not import infrastructure). Accepted as generated.
- **Lessons learned / limitations identified:** The re-validation gate on read is the third implementation of the same `GrafoTablero` + `esSolvable()` pattern (create, update, now read), confirming ADR-0001's defence-in-depth strategy is consistently enforced. No architectural violations were introduced — a marked improvement from T-004 where framework imports leaked into the application layer.

### T-009 — Implement Ticket 07: Architecture & ubiquitous-language guard tests

- **Task / problem addressed:** Build the automated rails from `.issues/07-architecture-language-guards.md`: domain imports no framework (ADR-0004), application never imports Prisma (PRD §7.7), and the PRD §4 avoid-list vocabulary (`CeldaSalida`, cell `*Decorator`, `Composite`, `NivelFacil/Medio/Dificil`, `PuntuacionPorTiempo`, plural `CargadorNiveles`) never reappears as a symbol — plus optional ESLint reinforcement.
- **AI tool used:** Claude Code (Sonnet 4.6), in this conversation, with the `tdd-strict`, `clean-architecture`, and `nestjs-patterns` skills explicitly invoked.
- **Prompt / instruction:** "Implement ticket 'c:/Users/jacjo/dev/arrowmaze-backend/.issues/07-architecture-language-guards.md' It is MANDATORY that you strictly apply 'tdd-strict', 'clean-architecture', and 'nestjs-patterns'" (verbatim, sent twice after an unrelated tool-permission interruption).
- **Result obtained:** Added `src/__arch__/source-scanner.ts` (shared file-scanning/import-extraction/identifier-tokenizing utility) and `forbidden-symbols.ts` (the framework-prefix and avoid-list consts, documented with ADR-0004/PRD §4 rationale per the ticket's refactor notes), plus three specs: `domain-purity.spec.ts`, `application-no-prisma.spec.ts`, and `ubiquitous-language.spec.ts`. Each guard was verified RED→GREEN by planting a real violation (a scratch file importing `@nestjs/common`, or defining `class CeldaSalida`) and watching the relevant spec fail, then deleting the scratch file. `application-no-prisma.spec.ts` went RED on the *current* tree (not a scratch file) because it caught two real, already-merged violations from Ticket 02: `RegisterUserUseCase` imported `@nestjs/common` (`@Injectable`/`@Inject`), and `application/dtos/register-user.dto.ts` carried `@nestjs/swagger` decorators with zero `class-validator` runtime validation. Both were fixed to match the established `CrearNivelCasoDeUso`/`CrearNivelRequestDto` convention: the use case is now a plain class wired via factory in `auth.module.ts`; the Swagger+validation DTO moved to `infrastructure/adapters/http/dtos/register-user-request.dto.ts`; `application/dtos/register-user.dto.ts` is now a plain interface. Running `auth.e2e-spec.ts` to verify the fix surfaced a third, unrelated pre-existing bug: `AuthModule` was never registered in `AppModule`, so `/auth/register` had been returning 404 since Ticket 02 merged — fixed with a one-line import. Also added ESLint reinforcement: extended `no-restricted-imports` to cover `src/application/**` (plus `bcrypt`/`rxjs`), and added `id-denylist` + a `no-restricted-syntax` regex rule for the avoid-list, confirmed firing via a scratch-file violation. Final state: 76/76 unit tests pass (up from 68), `npx eslint` exits 0.
- **Modifications made by the team:** <!-- DRAFT: confirm — not yet reviewed or committed at the time of writing. The AI made two scope decisions beyond the ticket's literal text that the team should explicitly ratify: (1) broadened Step 2 from "no Prisma" to "no forbidden framework + no infrastructure-layer imports," since the ticket's literal scope would have missed the real NestJS leak; (2) fixed the two pre-existing violations and the unrelated AuthModule wiring bug in the same pass rather than only flagging them, since the ticket's own "no production module" framing was in tension with actually making its own guards pass. -->
- **Lessons learned / limitations identified:** (1) A guard ticket's literal text can be narrower than the architectural rule it's meant to enforce — Step 2 as literally written ("no Prisma") would have shipped green while leaving a real `@nestjs/common` leak undetected; cross-checking ticket scope against the mandated architecture skill before implementing literally is what surfaced this. (2) Running the e2e suite (not just unit tests) to verify a fix is what exposed a completely unrelated bug (`AuthModule` missing from `AppModule`) — unit tests alone, even 76/76 green, would not have caught a dead route. (3) `npm test` had been silently masking a broken `node_modules` (missing `class-validator`/`class-transformer` despite being in `package.json`) because the unit-test rootDir never loads `app.module.ts`'s full provider graph; only `npm run test:e2e` surfaced it. (4) Full e2e DB-assertion coverage for `auth.e2e-spec.ts` could not be verified in this sandbox (no `DATABASE_URL`); only the routing/DI graph was confirmed correct.

### T-010 — Implement Ticket 06: Login + JWT guard + `ProveedorSesion` port

- **Task / problem addressed:** Build `.issues/06-login-jwt-guard-proveedor-sesion.md`: a `POST /auth/login` endpoint that verifies bcrypt-hashed credentials (reusing Ticket 02's `IHashContrasena` port) and issues a JWT via an **injected, request-scoped `ProveedorSesion` port** (ADR-0002 — no static/global session accessor), plus a `JwtAuthGuard` that protects routes by verifying the bearer token and populating that same port for downstream consumers (e.g. Ticket 09's `DecoradorSeguridad`).
- **AI tool used:** Claude Code (Sonnet 4.6)
- **Prompt / instruction:** "Implement ticket 06-login-jwt-guard-proveedor-sesion. It is MANDATORY that you strictly apply 'tdd-strict', 'clean-architecture', and 'nestjs-patterns'." (verbatim)
- **Result obtained:** 13 new files across all three Clean Architecture rings. Domain: `CredencialesInvalidasException` (uniform response for both unknown-email and wrong-password, preventing user enumeration). Application: `LoginUseCase` (loads user, compares hash, calls `proveedorSesion.guardarToken()` — never imports `@nestjs/jwt`), `login.dto.ts` (`LoginDto`/`LoginResultadoDto`), and `IProveedorSesion` port (`guardarToken` / `establecerPrincipal` / `obtenerToken` / `obtenerPrincipal` / `cerrarSesion`). Infrastructure: `JwtAdapter` (the sole `@nestjs/jwt` import site — `firmar`/`verificar`), `ProveedorSesionAdapter` (`@Injectable({ scope: Scope.REQUEST })` per ADR-0002, not a singleton), `JwtAuthGuard` (extracts the bearer token, 401 on missing/invalid, delegates verification to `JwtAdapter`, then calls `establecerPrincipal`), `LoginRequestDto` (class-validator), and `CredencialesInvalidasExceptionFilter` (→401). Wired in `AuthModule` (`JwtModule.registerAsync` via `ConfigService`, factory providers for `LoginUseCase` and `I_PROVEEDOR_SESION`) and `AuthController` (`POST /auth/login`, plus a guarded `GET /auth/me` probe route returning the current principal, matching the ticket's Step 3 acceptance test). Added 16 new unit tests across 4 spec files and 4 new e2e tests in `auth.e2e-spec.ts` (login success returns a 3-part JWT string, wrong password → 401, probe without token → 401, probe with issued token → 200 + principal). Verified in this session: `npm test` → 21 suites / 103 tests pass (up from 76 at T-009); the three architecture-guard specs (`domain-purity`, `application-no-prisma`, `ubiquitous-language`) still pass — `login.use-case.ts` and `proveedor-sesion.port.ts` import no NestJS/Prisma/bcrypt symbols.
- **Modifications made by the team:** Accepted as generated — no manual corrections to the AI's output.
- **Lessons learned / limitations identified:** (1) `AuthModule`'s `JwtModule.registerAsync` falls back to a hardcoded `DEFAULT_JWT_SECRET = 'dev-secret-change-me'` when the `JWT_SECRET` env var is unset, and that variable is referenced nowhere else in the repo (no `.env.example` entry) — if ever deployed without explicitly setting it, tokens would be silently signed with a publicly-known string. This was not caught by any test, since the test suite never exercises the "env var absent" path. (2) Could not run `npm run test:e2e` in this sandbox (no `DATABASE_URL`/`.env` present) — the same limitation noted in T-009 — so the four new login/probe e2e assertions were reviewed by reading the spec, not executed; only unit tests and the architecture guards were run directly.

### T-011 — Implement Ticket 09: Use-case Decorator stack (Métricas / Registro / Seguridad)

- **Task / problem addressed:** Build `.issues/09-use-case-decorator-stack.md` — the rubric's "AOP via SOLID, no library" showcase (ADR-0004): a common `ICasoDeUso<E,S>` surface, an abstract `DecoradorCasoDeUso` base, and three concrete decorators (`DecoradorMetricas`, `DecoradorRegistro`, `DecoradorSeguridad`) that add metrics/logging/security to an existing use case purely by composition, depending only on ports (`IMedidorMetricas`, `IRegistro`, `ProveedorSesion` from Ticket 06) — with real console output confined to infra adapters.
- **AI tool used:** Claude Code (Sonnet 4.6), in this conversation, with the `tdd-strict`, `clean-architecture`, and `nestjs-patterns` skills explicitly invoked.
- **Prompt / instruction:** "Implement ticket 09-use-case-decorator-stack  It is MANDATORY that you strictly apply 'tdd-strict', 'clean-architecture', and 'nestjs-patterns'." (verbatim)
- **Result obtained:** 17 new files, 1 file modified, via strict Red-Green-Refactor per the ticket's 4 steps. `ICasoDeUso<E,S>` (`application/ports/caso-de-uso.interface.ts`); abstract `DecoradorCasoDeUso<E,S>` delegating `execute`; `DecoradorMetricas` (records duration via `IMedidorMetricas`, even on failure, using try/finally); `DecoradorRegistro` (logs start/finish via `IRegistro`, order-tested: inicio → wrapped execute → fin); `DecoradorSeguridad` (reads the principal exclusively via the injected `IProveedorSesion.obtenerPrincipal()` — never a static accessor — throwing the new domain `NoAutorizadoException` when absent); `IMedidorMetricas`/`IRegistro` ports; infra adapters `MedidorMetricasSimple`/`RegistroConsola` (the only `console.log` call sites). `CrearNivelCasoDeUso` (Ticket 01) was given `implements ICasoDeUso<CrearNivelDto, CrearNivelResultadoDto>` with zero changes to its body, proving the ticket's "gains decorators without editing its own logic" requirement. Step 4's composition wiring is proven by `infrastructure/modules/decorator-stack.integration.spec.ts`, which resolves a `Seguridad→Registro→Metricas→CrearNivelCasoDeUso` stack from a real `@nestjs/testing` DI container and asserts: (a) the decorated result matches the bare use case's result, (b) calls happen in the order `registro:inicio → metricas → registro:fin`, (c) a session with no principal blocks before Registro/Metricas ever run. Verified: 28 suites / 117 tests pass (up from 103 at T-010); all three `__arch__` guards pass; `eslint` clean.
- **Modifications made by the team:** Accepted as generated, with two scope/design judgment calls the AI made that the team should explicitly ratify: (1) class names follow the ticket's own literal text (`DecoradorMetricas`/`DecoradorRegistro`/`DecoradorSeguridad`) rather than the `...CasoDeUso`-suffixed variant used elsewhere in `PRD.md` (`DecoradorMetricasCasoDeUso` etc.) — the two documents disagree, and the ticket (the concrete, file-level spec) was treated as authoritative; (2) the composed decorated use case is **not** wired into the live `LevelsModule`/`AuthModule` — it only exists inside the integration test — because doing so for real would require `LevelsModule` to import the entirety of `AuthModule` (JWT/Prisma/Config) just for the `ProveedorSesion` port, and would silently make `POST /levels` require authentication, a behavior change the ticket never asked for.
- **Lessons learned / limitations identified:** (1) The integration test for Step 4 was first written inside `src/application/decorators/`, since that's where the rest of the decorator code lives — but the existing `application-no-prisma.spec.ts` guard (built in T-009) immediately failed because the test imports `@nestjs/testing`, which matches the same `@nestjs/` forbidden-prefix rule used to keep the application layer framework-free. The guard doesn't special-case spec files, and that's correct: a DI-wiring test is itself an infrastructure-layer concern, so the file was relocated to `src/infrastructure/modules/`. This is the second ticket in a row (after T-009/T-010) where a Ticket-07 guard caught a real placement mistake before it needed a human reviewer to notice. (2) `DecoradorSeguridad.execute()` initially threw `NoAutorizadoException` from a non-`async` method; since a synchronous `throw` inside a non-async function propagates immediately to the caller instead of becoming a rejected `Promise`, the RED test's `await expect(...).rejects.toBeInstanceOf(...)` assertion would never have been reached — fixed by marking `execute` `async`, a reminder that any decorator returning `Promise<S>` that can throw before delegating must itself be `async`. (3) Ticket 09 explicitly depends on Ticket 06's `ProveedorSesion` port and Ticket 01's use case, and both slotted in without modification (beyond the one-line `implements` clause) — confirming the ports-and-adapters boundary drawn in Tickets 01/06 was genuinely stable enough to support the cross-cutting composition this ticket adds on top.

### T-012 — Implement Ticket 10: Backend domain-events publisher

- **Task / problem addressed:** Build `.issues/10-backend-domain-events-publisher.md`: registration emits a `JugadorRegistradoEvent` domain event, published via `IPublicadorEventos`, without `RegisterUserUseCase` knowing the transport (PRD §6.2 DM-B7) — explicitly distinct from the frontend's `PublicadorEventosJuego`. The ticket's own text assumed `AggregateRoot`/`DomainEvent` "stereotypes" already existed in `src/domain/stereotypes`; they didn't, so building them was a prerequisite.
- **AI tool used:** Claude Code (Sonnet 4.6), in this conversation, with the `tdd-strict`, `clean-architecture`, and `nestjs-patterns` skills explicitly invoked.
- **Prompt / instruction:** "Implement ticket 10-backend-domain-events-publisher. It is MANDATORY that you strictly apply 'tdd-strict', 'clean-architecture', and 'nestjs-patterns'." (verbatim; the user's terminal appended stray shell noise — `bash: Implement: command not found` — which was disregarded as not part of the instruction).
- **Result obtained:** 8 new files via Red-Green-Refactor, 4 files modified. `AggregateRoot`/`DomainEvent` stereotypes (`src/domain/stereotypes/`) built from scratch with `addDomainEvent`/`domainEvents`/`clearEvents()`, exactly as the ticket's Step 1 names them; `JugadorRegistradoEvent` (id + email only, no password); `IPublicadorEventos` port placed in `domain/events/` (a domain-layer port, mirroring repository ports, deliberately distinct in name and location from the frontend `PublicadorEventosJuego`); `User.registrar(...)` static factory that records the event, while the plain constructor (used by `UserPrismaMapper` to rehydrate existing rows) stays event-free so logins/loads never re-fire "player registered"; `RegisterUserUseCase` now injects `IPublicadorEventos`, publishes the recorded events after `save()` commits and clears them, publishing nothing on the duplicate-email path (asserted via spy); `PublicadorEventosAdapter` (console-based, the only `console.log` site, mirroring Ticket 09's `RegistroConsola`/`MedidorMetricasSimple` convention) wired into `auth.module.ts`. Verified: 31 suites / 128 tests pass (up from 117 at T-011); all `__arch__` guards pass; `eslint` clean.
- **Modifications made by the team:** Significant enough that this ticket is **not** accepted as fully AI-generated. After implementation, the team initiated a critical architecture review, asking the AI to "review the tests and analyze whether [`User` extending `AggregateRoot`] is necessary" — challenging whether a generic, reusable base class was a premature abstraction, since `User` is the only production consumer of `AggregateRoot`/`DomainEvent` and `aggregate-root.spec.ts` only exercises a synthetic test subclass (`AgregadoDePrueba`), a pattern the AI's own operating guidance otherwise flags as a smell. The AI's self-review conceded the critique was legitimate but recommended keeping the stereotype layer because the ticket text and PRD §6.2 DM-B7 explicitly name "`AggregateRoot`/`DomainEvent` stereotypes" as pre-existing, intentional architecture — i.e. the AI was fulfilling a named spec, not inventing the abstraction unprompted. The team accepted that recommendation and the code was kept as generated (no lines rewritten) — but the review itself is the team's substantive contribution: the AI did not surface this trade-off proactively; it only did so when directly challenged.
- **Lessons learned / limitations identified:** (1) "The ticket/PRD names this pattern explicitly, so it isn't a premature abstraction" is a justification the AI now reuses across tickets (T-009's decorator stack, this ticket's stereotypes) without first checking *how many concrete consumers exist today* — for both, the answer is exactly one. The AI should surface that count unprompted instead of waiting to be challenged. (2) This is the third ticket in a row (after T-007's "no Prisma" guard text and T-009/T-010's module-wiring assumptions) where the ticket's literal text didn't match the actual repository state — here, claiming stereotypes "already" existed when they had to be built from scratch. Ticket text should be treated as a *spec of intent*, verified against the repo, not as a *fact about current code*. (3) The ticket itself offered two implementation paths ("a `Jugador` aggregate, or extend `User` → `AggregateRoot`"); the AI picked the lower-blast-radius option (extending `User`) and only explained that choice after delivering the code, rather than flagging the trade-off up front — the same "don't silently pick the easiest literal option" pattern as (1), just one step earlier in the workflow.

### T-013 — Implement Ticket 08: Progress sync batch (`$transaction` inside the adapter)

- **Task / problem addressed:** Build `.issues/08-progress-sync-batch.md`: `POST /progress/sync` accepts a queue of completed offline runs and persists them as **one atomic batch** via a repository-scoped `$transaction` encapsulated inside `PrismaProgresoRepository.guardarLote` (ADR-0003 — no Unit of Work). Each run must be **re-scored server-side** via Ticket 05's `CalcularPuntuacionCasoDeUso` rather than trusting any client-claimed `{puntaje, estrellas}`, and the route is JWT-guarded per Ticket 06.
- **AI tool used:** Claude Code (Sonnet 4.6), in this conversation, with the `tdd-strict`, `clean-architecture`, and `nestjs-patterns` skills explicitly invoked.
- **Prompt / instruction:** "Implement ticket 08-progress-sync-batch It is MANDATORY that you strictly apply 'tdd-strict', 'clean-architecture', and 'nestjs-patterns'." (verbatim; the user's terminal again appended stray shell noise — `bash: Implement: command not found`, the same artifact seen in T-012 — disregarded as not part of the instruction).
- **Result obtained:** 12 new files via Red-Green-Refactor, 3 files modified. Domain: `Progreso` entity (plain class + `crear()` factory, no dedicated spec — covered indirectly via the use-case and repository specs, mirroring `Nivel`'s precedent) and `IRepositorioProgreso` port (`guardarLote`). Application: `SincronizarProgresoCasoDeUso` — for each run in the batch, fetches the `Nivel` via `IRepositorioNivel.obtenerPorId`, recomputes `{puntaje, estrellas}` via an injected `CalcularPuntuacionCasoDeUso` instance (spied/faked in tests, not a new port — reused as-is per the ticket's refactor note), and calls `guardarLote` exactly once with the full batch; throws `NivelNoEncontradoException` (reused from Ticket 01/04) before ever building the batch if any run names an unknown level, so a bad run blocks the whole sync with zero writes. `sincronizar-progreso.dto.ts` (plain interfaces, no class-validator — validation lives at the HTTP boundary). Infrastructure: `prisma/schema.prisma` gained a `Progreso` model (`jugadorId`→`User`, `nivelId`→`Nivel` FKs, both `onDelete: Cascade`); `ProgresoPrismaMapper.toCreateInput`; `PrismaProgresoRepository.guardarLote` wraps every `prisma.progreso.create(...)` call in a single `prisma.$transaction([...])` (array form, exactly as the ticket's Step 2 specifies); `SincronizarProgresoRequestDto`/`ProgresoEntradaRequestDto` (class-validator, whitelist-only — no `puntaje`/`estrellas` field exists on the wire contract at all, so the client cannot even attempt to assert a score); `ProgressController` (`POST /progress/sync`, `@UseGuards(JwtAuthGuard)`, `jugadorId` read from `IProveedorSesion.obtenerPrincipal()` — never from the request body, so a player cannot sync progress under another player's identity); `ProgressModule` wiring; `AuthModule` gained an `exports: [I_PROVEEDOR_SESION, JwtAdapter, JwtAuthGuard]` array (it had none before) so `ProgressModule` could reuse the existing guard/session machinery instead of re-wiring JWT verification from scratch — the first cross-module provider reuse in this codebase. `test/progress.e2e-spec.ts` covers: valid batch → 201 + row present with the server-recomputed score (not any client value, since none can even be sent); one run referencing an unknown level → 404 + **zero** rows for the whole batch (including the otherwise-valid run); no token → 401; empty batch while authenticated → 400 (`@ArrayMinSize(1)`). Verified: 33 suites / 136 tests pass (up from 31/128 at T-012); all `__arch__` guards pass (the new use case's own source was also asserted, via the same `fs.readFileSync` pattern as T-007, to contain no `prisma`/`$transaction` text); `eslint` clean after one `prettier` auto-fix pass.
- **Modifications made by the team:** Accepted as generated — no manual corrections after the AI's own self-review (below) was applied.
- **Lessons learned / limitations identified:** (1) The AI initially drafted the e2e "server overrides client score" test by sending `puntaje`/`estrellas` fields in the request body and asserting the response ignored them — but caught, before ever running it, that `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })` would reject those unknown fields with 400 instead of exercising the intended 201 path, since the request DTO never declares them. The test was rewritten to omit the fields entirely, which is in fact a *stronger* guarantee than the original test would have proven (the client cannot send a score at all, not merely "the server ignores it if sent") — caught by re-reading the DTO shape against the test body before execution, not by a failing run. (2) The AI initially registered `NivelNoEncontradoFilter` as a second `APP_FILTER` provider inside `ProgressModule`, duplicating a binding already made globally via `LevelsModule` (which `AppModule` always imports) — redundant rather than incorrect, but removed before running tests once noticed during a wiring re-read. (3) Confirmed via `git stash -u` (stashing tracked **and** untracked changes) against a clean `develop` baseline that `nest build` already fails with 4 pre-existing TypeScript errors unrelated to this ticket, and that `auth.e2e-spec.ts`/the new `progress.e2e-spec.ts` both fail identically on "Environment variable not found: DATABASE_URL" — i.e. this sandbox's inability to run DB-backed e2e tests is an environment limitation reconfirmed (not introduced) by this session, and the new e2e spec's correctness could only be verified by structural review + a clean-baseline diff, not by a green run. (4) This is the third ticket in a row to extend an existing module's surface for reuse rather than duplicating its wiring (after T-009's decorator composition and T-011's stack) — `AuthModule` had simply never needed an `exports` array before because no other module had previously depended on its providers; the absence of cross-module reuse this far into the project is itself a signal that each prior ticket's "thin vertical slice" scoping had not yet forced that question.

### T-014 — Implement Ticket 11: Leaderboard read projection with TTL cache + mover __arch__ a shared

- **Task / problem addressed:** Build `.issues/11-leaderboard-projection-cache.md`: a read-only `GET /leaderboard?idNivel=UUID&limite=N` endpoint returning the top players per level scored server-side, served via a TTL cache (~60s, keyed by `(idNivel, limite)`) at the HTTP interceptor level — no write path (PRD §9.5). Also move `src/__arch__/` inside `src/shared/` so `src/` shows only `application/`, `domain/`, `infrastructure/`, `shared/` at the top level.
- **AI tool used:** opencode (deepseek-v4-flash-free)
- **Prompt / instruction:** (1) "Explora el código existente para entender el estado actual del proyecto y la base sobre la que vamos a construir para el Ticket 11" (paraphrased) — (2) "Procede con la implementación, siguiendo TDD estricto: primero los tests (RED), luego la implementación (GREEN), y finalmente la refactorización (REFACTOR)." (paraphrased) — (3) "Pasa a aplicar la lógica de caché del ranking" (paraphrased) — (4) "Pasa a crear un test de arquitectura para el leaderboard read-only" (paraphrased) — (5) "Pasa a la implementación completa, creando los tests e2e, el controlador y el módulo." (paraphrased) — (6) "Analiza el Arrow Maze Backend y dime qué es: __arch__. ¿Dónde se puede mover para que se vea más limpio el orden de carpetas?" (paraphrased) — (7) "Muévela a la carpeta shared" (paraphrased) — (8) "Usa la skill 'ai-usage-doc' para documentar en el AI_USAGE.md todo el trabajo, los prompts y el resultado de este ticket." (paraphrased)
- **Result obtained:** 11 new files via strict TDD (Red-Green-Refactor per the ticket's 4 steps), 1 file modified, 6 files moved. New files: `IConsultaRanking` read port with `obtenerTop(idNivel, limite)` (application); `RankingDto`/`EntradaRankingDto` response DTOs (application); `ConsultaRankingPrisma` adapter with read-optimized Prisma query (order by `puntaje desc, completadoEn asc` for deterministic tie-breaking); `InterceptorCacheRanking` with in-memory Map and configurable `ttlMs` (default 60s) decorating `GET /leaderboard`; `LeaderboardQueryDto` (class-validator: `@IsUUID('all')`, `@IsNumberString()`) with Swagger docs; `LeaderboardController` (`@UseInterceptors(InterceptorCacheRanking)`) and `LeaderboardModule` wiring `I_CONSULTA_RANKING` to the Prisma adapter; `ranking-read-only.spec.ts` arch guard asserting `publicar`/`guardar`/`crear` never appear in ranking files. Modified: `src/app.module.ts` added `LeaderboardModule` import. Moved: 6 files from `src/__arch__/` to `src/shared/__arch__/` (with `__dirname` path fixes and updated self-exclusion filters in `ubiquitous-language.spec.ts` and `ranking-read-only.spec.ts`). Final test state: 36 suites / 150 unit tests pass (up from 128 at T-013 + post-move verification), all 5 leaderboard e2e pass, lint clean.
- **Modifications made by the team:** Three lint issues were fixed in the AI's output during the REFACTOR phase: (1) unused `SetMetadata` import removed from `interceptor-cache-ranking.spec.ts`; (2) unused variable `otroNivelId` removed from `consulta-ranking-prisma.spec.ts`; (3) stale debug `const res1` (leftover from exploratory `console.log`) removed from `leaderboard.e2e-spec.ts`. All caught by `npm run lint --fix` before final verification. No architectural violations were introduced — confirmed by the three existing arch guards (`application-no-prisma`, `domain-purity`, `ubiquitous-language`) which continued passing after the `__arch__` move.
- **Lessons learned / limitations identified:** (1) The `__arch__` move required careful path tracing (`__dirname` goes from `src/__arch__/` to `src/shared/__arch__/`, so each `join(__dirname, '..', ...)` needed an extra `'..'`). The self-exclusion filter in `ubiquitous-language.spec.ts` and `ranking-read-only.spec.ts` also needed updating from `startsWith('__arch__/')` to `startsWith('shared/__arch__/')`. A test run immediately after the move caught all path errors. (2) The `InterceptorCacheRanking` stores TTL as a public property (`public ttlMs`) rather than accepting it via constructor injection, because NestJS would try to resolve `number` as a DI token — a framework-specific workaround. (3) `limite` uses `@IsNumberString()` instead of `@Type(() => Number) + @IsInt()` because HTTP query params are strings — the idiomatic NestJS pattern for numeric query params. (4) The 5 e2e tests use `Test.createTestingModule` with `overrideProvider(I_CONSULTA_RANKING)` (following the `levels.e2e-spec.ts` mock pattern), requiring no database — the correct pattern for read-only query endpoints.

### T-015 — Implement Ticket 14: Dynamic board geometry (shaped boards) + arrow-length ≥ 2 invariant

- **Task / problem addressed:** Build `.issues/14-feat-backend-dynamic-board-geometry-arrow-length.md`: let levels be non-rectangular by marking bounding-box positions as *absent* (a shape mask, distinct from a transparent empty cell), make the solver and `DefinicionTablero` mask-aware, and add a domain invariant that no arrow may resolve to a single-cell move (minimum path length 2). Persistence must round-trip a sparse board without inventing filler cells, and creation must be rejected (422) for a length-1 arrow.
- **AI tool used:** Claude Code (Opus 4.8), in this conversation, with the `tdd-strict`, `clean-architecture`, and `nestjs-patterns` skills explicitly invoked.
- **Prompt / instruction:** "implement this ticket c:\\Users\\maria\\Desktop\\ArrowMaze\\arrowmaze-backend\\.issues\\14-feat-backend-dynamic-board-geometry-arrow-length.md follow the rules inside these skills …clean-architecture…SKILL.md …nestjs-patterns…SKILL.md …tdd-strict…SKILL.md" (verbatim) — followed by "could you provide the exact, complete JSON payloads I should paste into Swagger to test: The 422 error … The 201 success case …" (verbatim), then "the single-cell arrow payload ALSO returned a 201 Created instead of the expected 422 … This strongly suggests a Domain Bypass … Please fix the level creation flow…" (verbatim).
- **Result obtained:** 2 new files + 22 modified, via Red-Green-Refactor across the three TDD steps in the ticket. **Step 1 (mask-aware solver):** added a `CeldaAusente` (`tipo: 'ausente'`) cell concept + `FabricaCeldasEstandar.crearAusente()` (distinct from transparent `CeldaVacia`); extracted a shared `deltaDireccion()` helper into `direccion.ts` (single source of truth for ray steps, reused by solver and the new invariant); made `GrafoTablero.esRayoLibre` treat an absent cell as a shape-edge exit (the ray leaves the playable region) while empty stays transparent-up-to-a-wall; added shaped golden boards (triangle solvable/unsolvable, heart solvable) to the cross-repo `shared/__fixtures__/golden-boards.ts`. **Step 2 (arrow-length invariant):** new `FlechaLongitudInvalidaException` and a `LONGITUD_MINIMA_FLECHA = 2` named constant; `DefinicionTablero.crear` now rejects any arrow whose ray resolves to a single cell (first step leaves the box or lands on an absent cell), while `restaurar` deliberately trusts persisted data. **Step 3 (DTO/mapper/filter/e2e):** `CeldaDto` + both request DTOs accept `'ausente'`; `mapearCeldasDesdeDto` maps it; `NivelPrismaMapper` skips absent positions on write (no row) and reconstructs them as `ausente` over the full `ancho × alto` box on load; new `FlechaLongitudInvalidaFilter` → 422, registered in `LevelsModule`; e2e covers shaped-heart → 201 (absent cells preserved on the saved aggregate) and single-cell arrow → 422 with nothing persisted; added a repository-level sparse round-trip test. The new invariant invalidated many pre-existing 1×1-single-arrow test fixtures, which were migrated to length-valid boards. Verified: 38 suites / 166 unit tests pass (up from 150 at T-014), `levels.e2e-spec.ts` 12/12; `tsc --noEmit` clean and `eslint` clean on all touched files (two remaining lint errors in `auth.module.ts` and `decorator-stack.integration.spec.ts:90` were confirmed pre-existing via `git stash`).
- **Modifications made by the team:** The team manually tested the supplied Swagger payloads against the running server: the heart board returned 201 as expected, but the single-cell-arrow payload returned **201 instead of 422**, and the team flagged it as a suspected domain bypass in the application layer. Investigation (re-reading the create flow, then comparing `dist/` timestamps) showed the source and the full test suite were correct — the running server was executing a **stale compiled `dist/` build from before this ticket** (the new exception/filter `.js` files were absent from `dist/`, and the compiled `definicion-tablero.js` had zero trace of the invariant). The fix was operational, not code: `npm run build` to refresh `dist/`, then the team must restart/redeploy the server. (A separate, pre-existing gap was surfaced and offered but not yet applied: `main.ts` never registers the global `ValidationPipe` the `nestjs-patterns` skill prescribes — the e2e tests add it manually, masking the omission.)
- **Lessons learned / limitations identified:** (1) **Green tests ≠ correct runtime when a build artifact sits in between.** The AI verified the 422 path via e2e against the live `AppModule` and handed over Swagger payloads asserting "this returns 422" without warning that a server already running an older `dist/`/Docker image would not reflect the change until rebuilt and restarted. For any ticket that adds a *new* rejection path, the handover should explicitly call out "rebuild + restart the server (or redeploy the image) before testing," since the most likely cause of a "the fix doesn't work" report is a stale process — exactly what happened here, and it was initially misread as a domain bypass. (2) Introducing a construction-time invariant has a wide blast radius on existing fixtures: ~9 spec files plus the e2e and a shared golden fixture used 1×1 single-arrow boards that the new rule makes illegal, all of which had to be migrated in the same pass — a reminder that "the canonical minimal valid board" used across a test suite should be defined once and reused, not copy-pasted, so a future invariant change touches one place. (3) The serve path (`GET /levels/:id`) was deliberately **not** given a redundant length re-check: because a level can only ever be persisted via `crear` (which enforces the invariant), nothing with a single-cell arrow can exist to be served — the type-level guard is the single enforcement point, matching how the existing solvability gate is reasoned about.

### T-016 — Fix duplicate-`numero` 500 → 409 and wire the global `ValidationPipe`

- **Task / problem addressed:** A `POST /levels` request with an already-used `numero` (e.g. `numero: 9`) returned **500 Internal Server Error** instead of a meaningful status. Diagnose the 500 from the running backend container's logs, fix the underlying cause so the endpoint returns correct status codes, then wire the global `ValidationPipe` in `main.ts` that T-015 had flagged as a pre-existing gap (the e2e suite added it manually, masking its absence at runtime).
- **AI tool used:** Claude Code (Opus 4.8), in this conversation.
- **Prompt / instruction:** (1) "I'm trying to test the ticket 15 changes via Swagger. When I send a valid POST /levels request (e.g., with \"numero\": 9 and no limiteTiempo), the server returns a 500 Internal Server Error instead of 201. Please fetch and read the recent Docker logs for the backend container to find the exact stack trace … Identify if this is a Prisma runtime issue, an unhandled exception, a missing import, or a bug in the code you just wrote. Fix the underlying issue immediately so the endpoint returns the correct status codes." (verbatim) — (2) "Great catch! … I verified the fix and the API correctly returns a 409 Conflict now. Also, yes, absolutely wire up the global ValidationPipe in main.ts right now to ensure consistency between our e2e tests and runtime. After you do that, please finalize the AI_USAGE.md entry and commit …" (verbatim).
- **Result obtained:** `docker logs arrowmaze-backend-1` showed the 500 was a `PrismaClientKnownRequestError` with `code: 'P2002'`, `meta.target: ['numero']` thrown at `PrismaNivelRepository.guardar` — i.e. an **unhandled Prisma unique-constraint violation** (not a missing import or a domain bypass): `numero: 9` already existed, so `prisma.nivel.create()` rejected and Nest fell back to a generic 500. Fixed by following the codebase's established domain-exception→`@Catch`-filter pattern: new `NumeroNivelDuplicadoException` (domain), a `try/catch` in `PrismaNivelRepository.guardar` that detects `P2002` on the `numero` target and rethrows it as that domain exception (keeping Prisma confined to infrastructure, so the `application-no-prisma` guard stays green), a new `NumeroNivelDuplicadoFilter` mapping it to **409 Conflict**, and its registration as an `APP_FILTER` in `LevelsModule`. Verified end-to-end against the live container + DB: duplicate `numero` → **409** (`{"statusCode":409,"message":"Ya existe un nivel con el número 9","error":"Conflict"}`), a fresh valid timed level → **201**, and the `numero ≥ 10` without `limiteTiempo` rule still → 422; `prisma-nivel.repository.spec.ts` 4/4 pass. Separately, added the global `ValidationPipe` to `main.ts` with the exact options the e2e suite uses (`whitelist`, `forbidNonWhitelisted`, `transform`, `enableImplicitConversion`); after a container restart, confirmed runtime now matches the tests — a missing-fields body → **400** with per-field messages, and an unknown key → **400** `"property bogusField should not exist"` (previously both would have been accepted).
- **Modifications made by the team:** <!-- DRAFT: confirm — the team manually reproduced the 500 in Swagger, then independently verified via Swagger that the fix returns 409, and explicitly authorised wiring the global `ValidationPipe`. No corrections were made to the AI's generated code; confirm or add any you recall before finalising. -->
- **Lessons learned / limitations identified:** (1) The 500 was caused by **AI-written persistence code from an earlier ticket that only handled the happy path** — `guardar` translated no store-level errors, so the unique `numero` constraint (a real invariant) surfaced as an opaque 500 rather than a 409; whenever a column carries a `@unique`, the adapter that writes it should translate `P2002` to a typed domain error in the same change that introduces the constraint, not leave it for a bug report. (2) This session directly applied the gap T-015 had only *flagged* — the global `ValidationPipe` missing from `main.ts` — confirming the value of recording "offered but not yet applied" items in the usage log: the follow-up was a one-edit fix once someone acted on the note. (3) Because the source is bind-mounted on Windows, Nest's `--watch` did **not** receive inotify events for the host edits and never recompiled; a container restart was required to pick up the changes — the same class of "the running process is stale relative to the source" trap diagnosed in T-015, here hitting the file-watcher rather than a compiled `dist/`.

### T-017 — Implement Ticket 17: Proportional star rating from final score

- **Task / problem addressed:** Build `.issues/17-feat-backend-proportional-star-rating.md`: replace the three hand-tuned absolute `umbralEstrella*` thresholds with a star rating that is **proportional to the final `Puntaje`**, so `Estrellas` (1–3) track score as a fraction of the level's achievable maximum (`referencia`) and scale consistently across levels of different `baseNivel`. Bonus levels (Ticket 15) must still short-circuit to a non-scoring result before any star calculation. The cross-repo golden fixtures (`golden-scores.ts`) had to be regenerated for the proportional model in lockstep with frontend Ticket 19 (PRD §7.5 agreement).
- **AI tool used:** Claude Code (Opus 4.8), in this conversation, with the `tdd-strict`, `clean-architecture`, and `nestjs-patterns` skills explicitly invoked.
- **Prompt / instruction:** "implement this ticket c:\\Users\\maria\\Desktop\\ArrowMaze\\arrowmaze-backend\\.issues\\17-feat-backend-proportional-star-rating.md follow the rules inside this skills …nestjs-patterns …tdd-strict …clean-architecture" (verbatim).
- **Result obtained:** 2 files modified + the shared fixtures regenerated, via Red-Green-Refactor across the ticket's 3 steps. **Step 1 (proportional bands):** rewrote `CalcularPuntuacionCasoDeUso.calcularEstrellas` to map `proporción = puntaje / referencia` to stars by inclusive bands — `proporción >= 9/10 → 3★` (near-max), `>= 2/3 → 2★`, otherwise the `1★` minimum — comparing via exact integer cross-multiplication (`puntaje * den >= referencia * num`) to keep boundaries crisp and float-free; bands declared as named module constants (`BANDA_3_ESTRELLAS_NUM/DEN`, `BANDA_2_ESTRELLAS_NUM/DEN`) with the inclusivity documented. `referencia` (the achievable maximum) is derived from level data by replaying the *same selected strategy* with a perfect run (`estrategia.calcular(nivel, 0, nivel.limiteTiempo ?? 0)`), so it equals `baseNivel` for untimed levels and `baseNivel + limiteTiempo*ktiempo` for timed ones, and the algorithm swaps without any caller change (ticket Step 1 ♻️). A `referencia <= 0` guard returns the `1★` minimum instead of dividing by zero. The old `calcularEstrellas(puntaje, umbral1, umbral2)` absolute-threshold lookup was removed; `umbralEstrella*` remain on `Nivel` (still required by its factory) but no longer drive stars. **Step 2 (bonus short-circuit):** confirmed already satisfied — `ejecutar` returns `ResultadoPuntaje.noPuntuable()` on `nivel.esBonus` before strategy/score/star steps; the two existing bonus specs (`esPuntuable === false`, `puntaje`/`estrellas` both `null`) stay green unchanged. **Step 3 (cross-repo agreement):** regenerated `src/shared/__fixtures__/golden-scores.ts` to 10 proportional-model fixtures with each entry's `referencia` recorded in a comment and explicit boundary coverage (untimed 3★ lower edge at `proporción = 9/10`, 2★ just-below, 2★ lower edge at exactly `2/3`, 1★ just-below, plus timed perfect/2★/1★/floor cases); the generic agreement spec reproduces every `{esperadoPuntaje, esperadoEstrellas}`. The `GoldenScore` interface kept `umbralEstrella*` (now annotated as legacy/inert) since `Nivel.crear` still needs them. **Tests written first (RED) then implemented (GREEN):** replaced the use case's absolute-threshold `stars` describe block with a `proportional stars` block (7 boundary tests), confirmed it failed against the old lookup (4 failing), then implemented the mapping to green; likewise saw the 2 now-misvalued golden fixtures go RED before regenerating them. Verified: 38 suites / 180 unit tests pass (up from 166 at T-015), `tsc --noEmit` clean, and `eslint` clean on all three touched files. The `progress.e2e-spec.ts` expectation (untimed `990/1000 = 0.99 → 3★`) and the `sincronizar-progreso` unit spec (which mocks the calculator) were both confirmed unaffected by the star-meaning change.
- **Modifications made by the team:** <!-- DRAFT: confirm — the exact proportional bands (9/10 for 3★, 2/3 for 2★) are a backend-side choice that the ticket says must be fixed in the golden fixtures and agreed with frontend Ticket 19; confirm these bands match the frontend twin (or adjust both repos' fixtures together) before the PRD §7.5 agreement can go green. The e2e suite was not run in this session (no `DATABASE_URL` in the sandbox, a limitation reconfirmed since T-009). -->
- **Lessons learned / limitations identified:** (1) Deriving `referencia` by replaying the *same* strategy with a perfect-run input (rather than hand-coding `baseNivel + limiteTiempo*ktiempo` in the use case) keeps the reference maximum in lockstep with whatever formula a strategy uses, so a future scoring strategy needs no change to the star step — the ticket's "referencia derived from level data" refactor goal fell out of reuse rather than a parallel formula. (2) Comparing the proportion with integer cross-multiplication instead of floating `puntaje/referencia >= 2/3` avoids a real boundary bug: `600/900` and the literal `2/3` are not guaranteed bit-identical doubles, so a float compare could miss the inclusive `2★` lower edge that the boundary fixture (`untimed_2star_boundary`) pins exactly. (3) The change is a pure *meaning* shift with no signature change to `ResultadoPuntaje` or the use case's public surface, so its blast radius is limited to the star step and the shared fixtures — but precisely because the fixtures are a cross-repo contract, the backend cannot unilaterally declare the agreement green; the band choice is an open coordination item with frontend Ticket 19, flagged above rather than silently assumed.

### T-018 — Implement Ticket 16: Seed & serve 15+ ordered levels with scaling complexity

- **Task / problem addressed:** Build `.issues/16-feat-backend-seed-serve-15-levels-catalog.md`: author/seed **≥15 ordered levels** identified by integer `numero`, with a documented, monotonic complexity-scaling rule (more cells/arrows as `numero` grows), every board passing the Ticket 01 solvability gate and the Ticket 14 arrow-length-≥2 invariant; ≥1 bonus (non-scoring) level; the seed must be idempotent and author boards **through `CrearNivelCasoDeUso`** (gate not bypassed); and expose an ordered catalog endpoint `GET /levels` returning summary fields only (`{ numero, nombre, dificultad, esBonus, ancho, alto }`, plus `id` for the per-level fetch) with board cells absent (fetched on demand via `GET /levels/:id`, Ticket 04).
- **AI tool used:** Claude Code (Opus 4.8), in this conversation, with the `tdd-strict`, `clean-architecture`, and `nestjs-patterns` skills explicitly invoked.
- **Prompt / instruction:** "implement this ticket c:\\Users\\maria\\Desktop\\ArrowMaze\\arrowmaze-backend\\.issues\\16-feat-backend-seed-serve-15-levels-catalog.md follow the rules in these skills …tdd-strict …nestjs-patterns …clean-architecture" (verbatim).
- **Result obtained:** 9 new files + 4 modified, via Red-Green-Refactor across the ticket's 3 steps. **Step 1 (complexity profile):** new pure domain service `perfilDificultad(numero)` (`src/domain/services/perfil-dificultad.ts`) returning `{ numero, ancho, alto, celdas, flechas, cronometrado }`, where board edges grow one step every two ordinals (`ancho = alto = 3 + floor((numero-1)/2)`), `celdas = ancho*alto`, `flechas = (ancho-1)*alto`, and `cronometrado = numero >= PRIMER_NIVEL_CRONOMETRADO` (imported from the `Nivel` aggregate so the timed boundary stays a single source of truth). 9 unit tests assert monotonic non-decreasing cells/arrows across 1…15, level 10 ≥ level 1, a strict increase 1→15, the timed/untimed boundary, and geometry consistency; no I/O so the frontend generator can reuse it. **Step 2 (seed integrity):** `prisma/catalogo-niveles.ts` (`construirCatalogoNiveles()` — pure data derivation building 15 `CrearNivelDto`s from the profile; each board is a full grid of `DERECHA` arrows with a trailing empty column, which guarantees a ≥2-cell ray per arrow and right-to-left solvability for any `ancho ≥ 2`; `numero 5` flagged `esBonus`; difficulty banded FÁCIL/MEDIO/DIFÍCIL; timed budget for `numero ≥ 10`) and `prisma/seed.ts` (exports a testable `sembrarCatalogo(autor, numerosExistentes)` that authors only missing `numero`s through `CrearNivelCasoDeUso` — so re-running is idempotent and the gate is never bypassed — plus a `require.main` bootstrap wiring `PrismaService` + `PrismaNivelRepository` + `CryptoGeneradorIdAdapter`). `test/seed.e2e-spec.ts` exercises the seed through the **real** `CrearNivelCasoDeUso` with an in-memory repository (no DB needed): asserts ≥15 levels, distinct consecutive `numero` 1…N, every board re-passes `esSolvable` (and implicitly the arrow-length gate, since the use case throws otherwise), ≥1 bonus, the timed-by-ordinal rule, and idempotency on a second run (0 created, all omitted). `package.json` gained `"seed"` script + a `prisma.seed` entry (`ts-node prisma/seed.ts`). **Step 3 (catalog endpoint):** `NivelResumenDto` (application), `IListarNiveles`/`I_LISTAR_NIVELES` read port in a new `src/application/queries/` folder (read-only — no write methods), `ListarNivelesPrisma` adapter (`findMany` with `orderBy: { numero: 'asc' }` and a `select` projection that excludes `celdas`), wired into `LevelsModule` and a new `@Get()` route on `LevelsController` (injected via the port symbol). 4 adapter unit tests assert the ordering + summary-only projection; 3 new `levels.e2e-spec.ts` tests (overriding `I_LISTAR_NIVELES`) assert `GET /levels` returns the catalog ordered ascending, with exactly the summary fields and no `celdas`, and flags bonus levels. Verified: 40 suites / 193 unit tests pass (up from 38/180 at T-017), `levels` + `seed` e2e 26/26 pass, `tsc --noEmit` clean, and `eslint` clean on all touched files (auto-fixed two prettier wraps and replaced three `async`-without-`await` stubs in the in-memory test double with explicit `Promise.resolve`).
- **Modifications made by the team:** <!-- DRAFT: confirm — the real `prisma/seed.ts` was NOT run against the live database in this session (it would mutate the dev DB); seed integrity was proven against an in-memory repository through the real create use case. Run `npm run seed` (idempotent) to populate the actual DB, then confirm `GET /levels` returns the 15 ordered levels. Also confirm the summary DTO including `id` (needed for the per-level `GET /levels/:id` fetch) is acceptable, since the ticket's field list named the six gameplay fields but not `id`. -->
- **Lessons learned / limitations identified:** (1) The "genuinely solvable, not 15 copies of level 01" requirement (the content-debt the frontend Ticket 13 flagged) was met structurally rather than by hand-authoring: a full-arrow grid with one trailing empty column is provably solvable for *any* size, so the same construction scales across all 15 ordinals while complexity still rises via `PerfilDificultad` — making "add level 16" a one-line `TOTAL_NIVELES` bump (data, not code), exactly the ticket's ♻️ goal. (2) Idempotency was placed in the seed *runner* (skip existing `numero`s), not in the catalog *data* or the repository, so it is unit-testable without a DB and does not depend on the `@unique`-constraint→409 path added in T-016; this keeps the "author through the gate" guarantee intact (existing levels are skipped before `execute`, never re-created). (3) Ordering is enforced in the Prisma adapter (`orderBy`) and verified by an adapter unit test rather than at the controller, so the e2e test (which mocks the read port) covers wiring + projection shape while the unit test covers the actual sort — the same read-port split used for the leaderboard in T-014. (4) Sandbox note reversed from prior tickets: a `DATABASE_URL` *was* reachable this session (Prisma connected during the `levels` e2e via `AppModule`'s real `onModuleInit`), but the DB-mutating seed was still left unrun by choice to avoid an irreversible write to the shared dev database.

## 3. Critical Evaluation

### AI-assisted code share

- **Approximate % of code that was AI-assisted:** ~92% by file/line authorship (cumulative across all tickets) — but see the caveat below; this metric does not capture T-012's human-led design review.
- **Basis for the estimate:** 104 AI-assisted files out of 113 total `.ts` files (`src/` + `test/`) as of T-012 (Ticket 10). T-013 (Ticket 08) adds 12 new AI-generated files and modifies 2 existing AI-assisted files plus the non-`.ts` `prisma/schema.prisma`. T-014 (Ticket 11 + mover `__arch__`) adds 11 new AI-generated files and modifies 1 existing AI-assisted file (`app.module.ts`). T-015 (Ticket 14) adds 2 new AI-generated files (`flecha-longitud-invalida.exception.ts`, `flecha-longitud-invalida.filter.ts`) and modifies 22 existing AI-assisted files. T-016 adds 2 new AI-generated files (`numero-nivel-duplicado.exception.ts`, `numero-nivel-duplicado.filter.ts`) and modifies 3 existing AI-assisted files (`prisma-nivel.repository.ts`, `main.ts`, `levels.module.ts`). Combined: 133 AI-assisted files out of 144 total `.ts` files (recount via `find src test -name "*.ts" | wc -l`); the hand-authored non-AI baseline (~11 config/scaffold files) is unchanged → 133/144 ≈ 92.4%. T-018 (Ticket 16) adds 9 new AI-generated files (`src/domain/services/perfil-dificultad.ts` + spec, `src/application/dtos/nivel-resumen.dto.ts`, `src/application/queries/listar-niveles.interface.ts`, `src/infrastructure/adapters/persistence/queries/listar-niveles-prisma.ts` + spec, `prisma/catalogo-niveles.ts`, `prisma/seed.ts`, `test/seed.e2e-spec.ts`) and modifies 3 AI-assisted `.ts` files (`levels.controller.ts`, `levels.module.ts`, `levels.e2e-spec.ts`) plus the non-`.ts` `package.json`.

### Incorrect or suboptimal AI results

- **Case:** NestJS decorators and imports (`@Injectable`, `@Inject`, `@nestjs/common`) were placed inside `CrearNivelCasoDeUso` in the application layer, violating the Clean Architecture dependency rule (application must not import framework code).
  - **How it was detected:** Code review by the team flagged the `@nestjs/common` import.
  - **How it was corrected:** Replaced with plain class + constructor injection; NestJS injection wiring moved to the infrastructure module.

- **Case:** Solvable/unsolvable board fixture data was written inline inside `nivel.spec.ts` instead of in the shared golden-fixtures module, making it impossible for the frontend (Dart) to reference the same boards for solver agreement testing.
  - **How it was detected:** Code review — team noticed the boards were not in `shared/__fixtures__/`.
  - **How it was corrected:** Moved board definitions to `src/shared/__fixtures__/golden-boards.ts` and imported them from tests.

- **Case (T-005):** The shared helper `mapearCeldasDesdeDto` was placed inside `actualizar-nivel.use-case.ts` and imported by `crear-nivel.use-case.ts`, coupling two independent vertical slices. A utility function should live in a shared location, not inside a use case.
  - **How it was detected:** Architecture analysis post-implementation flagged the cross-use-case dependency.
  - **How it was corrected:** Flagged for refactoring to `src/application/utils/mapear-celdas.ts` — not yet applied.

- **Case (T-006):** The unused `umbral3` parameter was included in `CalcularPuntuacionCasoDeUso.calcularEstrellas()`. The method only needs `umbral1` and `umbral2` (3-star and 2-star thresholds) since 1 star is the implicit floor. The extra parameter was dead code.
  - **How it was detected:** ESLint `@typescript-eslint/no-unused-vars` flagged it on `npm run lint`.
  - **How it was corrected:** Removed `umbral3` from the `calcularEstrellas` signature and its call site.

- **Case (T-007):** The commit implementing bcrypt hashing (`f3e7078`) depends on four files (`User` entity, `RegisterUserDto`, `IUserRepository`, `UserPrismaMapper`) that had been deleted by an earlier "purge boilerplate" commit (`c6bd26e`) and were recreated in the same AI session but never staged — leaving the bcrypt commit's dependencies untracked.
  - **How it was detected:** `git status` on the branch showed the four files as untracked while the commit that imports them was already in history.
  - **How it was corrected:** Committed separately as `feat(auth): add user entity, dto, repository interface and prisma mapper` (`d4018c1`), split from the `docs(ai-usage)` commit per `conventional-commits` guidance to keep the feature-completion and documentation changes independently reviewable.

- **Case (T-007):** The "use case must not import `bcrypt`" invariant (an explicit Definition-of-Done item in Ticket 02) was tested by having `register-user.use-case.spec.ts` read its own source file with `fs.readFileSync` and regex-match for `/bcrypt/i`, rather than via a structural/import-boundary check.
  - **How it was detected:** Code review while drafting this AI usage entry.
  - **How it was corrected:** Replaced by the general-purpose `application-no-prisma.spec.ts` guard added in T-009, which checks every application file (not just one) for the full forbidden-framework prefix list (including `bcrypt`) via the shared scanner rather than a single file's own source text.

- **Case (T-007, discovered while implementing T-009):** `AuthModule` (added in the Ticket 02 session) was never registered in `AppModule`'s `imports` array, so `POST /auth/register` had been returning 404 since that commit merged to `develop` — the endpoint was dead despite all of its own unit and e2e tests existing.
  - **How it was detected:** Running `npm run test:e2e` while verifying the T-009 architecture fixes; `auth.e2e-spec.ts` expected 201/409 but got 404.
  - **How it was corrected:** Added `AuthModule` to `AppModule`'s `imports` in `src/app.module.ts`. This also reveals that `npm test` (unit-only) had never exercised the full module graph, so the dead route went undetected for an entire ticket cycle.

- **Case (T-009):** `ubiquitous-language.spec.ts`'s scanner tokenizes raw file text without stripping string literals or comments first, so a legitimate comment that happens to spell out a banned word verbatim (e.g. explaining *why* a field is not called `CeldaSalida`) would false-positive as a violation anywhere outside the excluded `src/__arch__/` directory.
  - **How it was detected:** Self-review of the scanner's design while writing this entry — not caught by a failing test, since no such comment exists in the current tree.
  - **How it was corrected:** Not corrected — documented as a known limitation. A future refinement would strip `//` and `/* */` comments and string-literal contents before tokenizing.

- **Case (T-010):** `AuthModule`'s `JwtModule.registerAsync` factory falls back to a hardcoded literal, `DEFAULT_JWT_SECRET = 'dev-secret-change-me'`, whenever the `JWT_SECRET` environment variable is unset. The variable is not referenced anywhere else in the repository (no `.env.example` entry, no config validation), so a deployment that forgets to set it would silently sign and verify all tokens with a publicly-known string instead of failing fast.
  - **How it was detected:** Self-review while writing this entry — not caught by any test, since the suite never exercises the "env var absent in a real deployment" path (tests run with the same in-process default).
  - **How it was corrected:** Not corrected — documented as a known limitation. A future fix would have `ConfigService` (or a startup check) throw if `JWT_SECRET` is missing, rather than defaulting silently.

- **Case (T-011):** The Step-4 composition-wiring integration test was first placed in `src/application/decorators/`, alongside the decorators it exercises, and imported `@nestjs/testing` to resolve a Nest DI container.
  - **How it was detected:** Not by human review — the existing `application-no-prisma.spec.ts` architecture guard (added in T-009) failed immediately on `npm test`, flagging `@nestjs/` as a forbidden import inside `application/`.
  - **How it was corrected:** Relocated the test (with corrected relative import paths) to `src/infrastructure/modules/decorator-stack.integration.spec.ts`, since DI-container wiring is properly an infrastructure-layer concern; all guards and the full suite pass afterward.

- **Case (T-012):** `AggregateRoot`/`DomainEvent` were introduced as a generic, reusable base-class hierarchy, but `User` is the only production consumer, and the dedicated `aggregate-root.spec.ts` only exercises a synthetic test subclass written purely to test the base class — a single-consumer abstraction that the AI's own operating guidance (no abstraction without a real second consumer) would otherwise flag.
  - **How it was detected:** Not by a test or lint rule — the human reviewer directly challenged the design, asking the AI to "review the tests and analyze whether [`User` extending `AggregateRoot`] is necessary," rather than the AI raising the trade-off unprompted.
  - **How it was corrected:** Not corrected — after analysis weighing reuse-readiness against YAGNI, the team explicitly decided to keep the stereotype layer, since the ticket text and PRD §6.2 DM-B7 name it as intentional, reusable architecture rather than the AI's own invention. Documented as a reviewed-and-ratified trade-off, not a code change.

- **Case (T-013):** The first draft of the e2e "server overrides client score" test sent `puntaje`/`estrellas` fields in the `POST /progress/sync` request body, intending to assert the response used the recomputed values instead. The actual request DTO never declares those fields, so with `forbidNonWhitelisted: true` the request would have been rejected with 400 before reaching the use case at all — the test as drafted would have failed for the wrong reason and proven nothing about score recomputation.
  - **How it was detected:** Self-review of the DTO shape against the drafted test body, before ever running it — not by a failing test.
  - **How it was corrected:** Removed the fields from the test request; the assertion now relies on the DTO's whitelist itself being the (stronger) guarantee that a client cannot assert a score at all.

- **Case (T-013):** `ProgressModule` was initially given its own `{ provide: APP_FILTER, useClass: NivelNoEncontradoFilter }` binding, duplicating a global filter registration that `LevelsModule` already contributes (and which `AppModule` always loads).
  - **How it was detected:** Self-review of the module's provider list against the rest of the wiring, before running tests.
  - **How it was corrected:** Removed the duplicate provider from `ProgressModule`; the existing global binding from `LevelsModule` already covers `NivelNoEncontradoException` thrown anywhere in the app.

- **Case (T-015):** After implementing Ticket 14's arrow-length invariant, the AI handed the team exact Swagger payloads stating the single-cell-arrow body "returns 422" — verified true via e2e against the live `AppModule` — but did not warn that a server already running an older compiled `dist/` (or Docker image) would not pick up the change until rebuilt and restarted. The team tested against such a stale process and got 201, reasonably concluding a domain bypass existed in the application layer.
  - **How it was detected:** The team reported the 201 and hypothesised a bypass; the AI re-read the create flow (which correctly routes through `DefinicionTablero.crear`) and the passing e2e, then compared `dist/` timestamps — the compiled build predated the ticket and the new exception/filter `.js` files were absent from `dist/` entirely.
  - **How it was corrected:** Ran `npm run build` to refresh `dist/`; the running server must then be restarted/redeployed. No source change was needed — the code and tests were already correct. The lesson (warn about rebuild/restart whenever a ticket adds a new rejection path) is recorded in the T-015 log.

- **Case (T-016):** `PrismaNivelRepository.guardar` (AI-written in an earlier ticket) handled only the happy path: it never translated store-level errors, so a `POST /levels` with a `numero` that already exists hit the `@unique` constraint on `numero` and surfaced the raw `PrismaClientKnownRequestError` (`P2002`) as an opaque **500** instead of a meaningful **409 Conflict**.
  - **How it was detected:** A team member testing in Swagger reported the 500; reading `docker logs` for the backend container revealed the `P2002` / `target: ['numero']` stack trace originating in `guardar`.
  - **How it was corrected:** Added `NumeroNivelDuplicadoException`, a `try/catch` in `guardar` that rethrows `P2002`-on-`numero` as that domain exception (Prisma stays in infrastructure), and a `NumeroNivelDuplicadoFilter` → 409 registered in `LevelsModule`; verified live (409 on duplicate, 201 on a fresh level) and via the repository unit spec.

- **Case (T-016):** `main.ts` never registered the global `ValidationPipe` that the `nestjs-patterns` skill prescribes — a gap that had existed since project setup and was only *flagged* (not fixed) in T-015. The e2e suite added the pipe manually in each test, so the suite stayed green while the real server performed no DTO validation (unknown keys accepted, no field-level checks).
  - **How it was detected:** Carried over as a known gap from T-015's log; the team explicitly authorised fixing it this session.
  - **How it was corrected:** Added `app.useGlobalPipes(new ValidationPipe({ whitelist, forbidNonWhitelisted, transform, transformOptions: { enableImplicitConversion: true } }))` to `main.ts`, matching the e2e config exactly; verified at runtime that a missing-fields body and an unknown key now both return 400.

### Team reflection

- **Impact on productivity:** Tickets 01 and 03 were each implemented in single sessions with all layers (domain → application → infrastructure → tests) generated by AI, each would have taken several days manually. The reuse pattern (shared `mapearCeldasDesdeDto`) eliminated duplicated mapping code between create and update use cases in minutes.
- **Impact on code quality:** High for boilerplate (VOs, DTOs, mappers, repository adapters) and for enforcing the solvability invariant across all three paths (create, update, read) — no divergence between the three gates, confirming ADR-0001's defence-in-depth strategy. However, the AI co-locates shared helpers inside use-case files rather than extracting them to shared modules, and the upsert-via-existence-check pattern introduces an extra query. Both are design smells that require human oversight. Ticket 02's bcrypt port/adapter pattern was applied cleanly, but the untracked dependency files and brittle text-grep invariant test were quality concerns that human/AI review later caught and fixed (T-009).
- **Overall takeaways:** AI is excellent for rapidly scaffolding full vertical slices, including later slices (GET by id, architecture guards) that mirror or build on earlier ones with minimal new concepts. The third implementation of the solvability gate on read (T-008) was generated with zero architectural violations — a clear improvement from T-004. Layer-enforcement linting (Ticket 07 / T-009) and a stronger shared-utility extraction convention in AGENTS.md would catch the cross-use-case coupling pattern at write time. The TDD + AI codegen + human architecture review workflow remains effective — the architecture review after implementation caught the coupling issue that pure testing did not, and building Ticket 07's guards (T-009) paid for itself immediately: the moment they existed, they caught two real architecture violations and, indirectly via e2e verification, a dead route (`AuthModule` never registered) — all inherited from a previous AI session (T-007) that 76 passing unit tests had not caught. This is the strongest evidence yet that unit-test-green is not the same as architecturally-correct or functionally-reachable, and that AI sessions need an explicit "is the tree self-consistent" check (e.g. a clean-clone build, or running e2e, not just unit tests) before a ticket is considered done.
